[project]
name = "gaurav-quarto-website"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "pandas>=2.0",
    "jupyter>=1.0",
    "nbformat>=5.0",
    "ipython>=8.0",
]
